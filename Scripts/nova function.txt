CREATE OR REPLACE FUNCTION pessoa_existe(cpf_input VARCHAR)
RETURNS BOOLEAN AS $$
BEGIN
    RETURN EXISTS (
        SELECT 1
        FROM cliente
        WHERE cpf = cpf_input
    );
END;
$$ LANGUAGE plpgsql;


SELECT pessoa_existe('43300674808')